/*const mysql = require('mysql');
const con = mysql.createConnection({
    host: "localhost",
    user: "root",
    password: "",
    database: "test"
});
con.connect(function(err) {
    if (err) throw err;
    console.log("Connected!");
});

con.query("SELECT text FROM markery", function (err, result, fields) {
    if (err) throw err;
exports.array=result;

});

L.marker(latlng).addTo(map).bindPopup(feature.properties.name);
    con.end();*/
